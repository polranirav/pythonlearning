name = 'nirav polara'
print(name[0]) 
# Strings are immutable in Python, so you cannot change a character in a string directly
name[0] = 'j'
print(name[0])# This will raise an error because strings are immutable in Python
